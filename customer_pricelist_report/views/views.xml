<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="customer_pricelist_report.list">
      <field name="name">customer_pricelist_report list</field>
      <field name="model">customer_pricelist_report.customer_pricelist_report</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="customer_pricelist_report.action_window">
      <field name="name">customer_pricelist_report window</field>
      <field name="res_model">customer_pricelist_report.customer_pricelist_report</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="customer_pricelist_report.action_server">
      <field name="name">customer_pricelist_report server</field>
      <field name="model_id" ref="model_customer_pricelist_report_customer_pricelist_report"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="customer_pricelist_report" id="customer_pricelist_report.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="customer_pricelist_report.menu_1" parent="customer_pricelist_report.menu_root"/>
    <menuitem name="Menu 2" id="customer_pricelist_report.menu_2" parent="customer_pricelist_report.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="customer_pricelist_report.menu_1_list" parent="customer_pricelist_report.menu_1"
              action="customer_pricelist_report.action_window"/>
    <menuitem name="Server to list" id="customer_pricelist_report" parent="customer_pricelist_report.menu_2"
              action="customer_pricelist_report.action_server"/>
    -->
  </data>
</odoo>