<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="pricelist_clustering_based.list">
      <field name="name">pricelist_clustering_based list</field>
      <field name="model">pricelist_clustering_based.pricelist_clustering_based</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="pricelist_clustering_based.action_window">
      <field name="name">pricelist_clustering_based window</field>
      <field name="res_model">pricelist_clustering_based.pricelist_clustering_based</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="pricelist_clustering_based.action_server">
      <field name="name">pricelist_clustering_based server</field>
      <field name="model_id" ref="model_pricelist_clustering_based_pricelist_clustering_based"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="pricelist_clustering_based" id="pricelist_clustering_based.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="pricelist_clustering_based.menu_1" parent="pricelist_clustering_based.menu_root"/>
    <menuitem name="Menu 2" id="pricelist_clustering_based.menu_2" parent="pricelist_clustering_based.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="pricelist_clustering_based.menu_1_list" parent="pricelist_clustering_based.menu_1"
              action="pricelist_clustering_based.action_window"/>
    <menuitem name="Server to list" id="pricelist_clustering_based" parent="pricelist_clustering_based.menu_2"
              action="pricelist_clustering_based.action_server"/>
    -->
  </data>
</odoo>